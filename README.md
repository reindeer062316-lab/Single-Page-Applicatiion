# 數位名片盒（Single Page Application）

## 直接開啟

1. 進入此資料夾
2. 直接雙擊 `index.html` 就能看到名片頁

## 換成你的照片

- 方式 A（最簡單）：在頁面上按「換照片」選擇圖片檔
- 方式 B（固定預設圖）：把你的照片放到 `assets/`，並在 `index.html` 把
  `./assets/profile.svg` 改成你的檔名（例如 `./assets/me.jpg`）

